<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Customize your theme here. -->
        <!-- Base application thème. -->
    </style>

    <style name="Logactivity" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:background">@color/quantum_white_100</item>

        <!-- Customize your theme here. -->
        <!-- Base application thème. -->
    </style>

    <style name="ToolbarTheme" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/colorAccent</item>
        <item name="titleTextAppearance">@style/TextToolbar</item>
    </style>

    <style name="TextToolbar" parent="ToolbarTheme">
        <item name="title">@string/app_name</item>
        <item name="android:textColor">@color/quantum_white_100</item>
        <item name="android:background">@color/colorAccent</item>
        <item name="android:textSize">20sp</item>

    </style>

    <style name="NavigationDrawerStyle">

        <item name="android:textSize">17sp</item><!-- text size in menu-->

    </style>


</resources>
